function solve(pages, pagesPerHour, days) {

    let totalTime = (pages / pagesPerHour);
    let output = hoursPerDay = totalTime / days;

    console.log(output);
    
}

solve(212, 25 , 2);